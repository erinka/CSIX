<h1>Listing posts</h1>


<% @posts.each do |post| %>
  
    <h3><%=h post.title %></h3>
    
    <p><%=h post.body %></p>
    
   
    
    <span style="float:right"><%= link_to 'Show', post %> | <%= link_to 'Edit', edit_post_path(post) %> | <%= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete %></span>
  
<% end %>
</table>

<br />

<%= link_to 'New post', new_post_path %>
